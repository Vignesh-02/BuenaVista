<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BuenaVista - Discover Amazing Locations</title>
    <link rel="icon" type="image/png" href="/favicon.png">
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/stylesheets/landing.css">
    
    <!-- Theme Initialization (for consistency across pages) -->
    <script>
        (function() {
            const savedTheme = localStorage.getItem('theme');
            const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
            const theme = savedTheme || (prefersDark ? 'dark' : 'light');
            document.documentElement.setAttribute('data-theme', theme);
        })();
    </script>
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top landing-navbar">
        <div class="container" style="max-width: 1200px;">
            <a class="navbar-brand" href="/">
                <img src="/favicon.png" alt="BuenaVista" width="32" height="32" class="d-inline-block align-text-top">
                BuenaVista
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#landingNav" aria-controls="landingNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="landingNav">
                <ul class="navbar-nav ms-auto align-items-center">
                    <li class="nav-item">
                        <a class="nav-link" href="/locations">
                            <i class="bi bi-compass me-1"></i>Explore
                        </a>
                    </li>
                    <!-- Theme Toggle -->
                    <li class="nav-item">
                        <button class="theme-toggle-landing" id="themeToggle" aria-label="Toggle dark/light mode" title="Toggle theme">
                            <i class="bi bi-moon-fill"></i>
                            <i class="bi bi-sun-fill"></i>
                        </button>
                    </li>
                    <% if(!currentUser) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/login">Login</a>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-outline-light ms-lg-2" href="/register">Sign Up</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <span class="nav-link" style="color: var(--color-primary-light);">
                                <i class="bi bi-person-circle me-1"></i>
                                <%= currentUser.username %>
                            </span>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-outline-light ms-lg-2" href="/logout">Sign Out</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>
    
    <!-- Toast Container -->
    <div class="toast-container position-fixed top-0 end-0 p-3" style="z-index: 1100; margin-top: 70px;">
        <% if(error && error.length > 0) { %>
        <div class="toast toast-error show" role="alert" aria-live="assertive" aria-atomic="true">
            <div class="toast-header bg-danger text-white">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>
                <strong class="me-auto">Error</strong>
                <!-- <small>Just now</small> -->
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
            <div class="toast-body">
                <%= error %>
            </div>
        </div>
        <% } %>
        
        <% if(success && success.length > 0) { %>
        <div class="toast toast-success show" role="alert" aria-live="assertive" aria-atomic="true">
            <div class="toast-header bg-success text-white">
                <i class="bi bi-check-circle-fill me-2"></i>
                <strong class="me-auto">Success</strong>
                <!-- <small>Just now</small> -->
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="toast" aria-label="Close"></button>
            </div>
            <div class="toast-body">
                <%= success %>
            </div>
        </div>
        <% } %>
    </div>
    
    <!-- Hero Section -->
    <section id="landing-header">
        <div class="container" style="max-width: 1200px;">
            <div class="hero-content">
                <h1 class="hero-title">
                    Discover Your Next<br>
                    <span class="text-accent">Adventure</span>
                </h1>
                <p class="hero-subtitle">
                    Explore breathtaking locations around the world. 
                    Share your favorite spots and connect with fellow adventurers.
                </p>
                <div class="hero-buttons">
                    <a href="/locations" class="btn btn-primary btn-lg">
                        <i class="bi bi-compass me-2"></i>Explore Locations
                    </a>
                    <% if(!currentUser) { %>
                        <a href="/register" class="btn btn-outline-light btn-lg">
                            <i class="bi bi-person-plus me-2"></i>Join Free
                        </a>
                    <% } %>
                </div>
                
                <!-- Stats -->
                <div class="hero-stats">
                    <div class="stat-item">
                        <span class="stat-number">15+</span>
                        <span class="stat-label">Locations</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-number">10+</span>
                        <span class="stat-label">Explorers</span>
                    </div>
                    <div class="stat-item">
                        <span class="stat-number">8+</span>
                        <span class="stat-label">Countries</span>
                    </div>
                </div>
            </div>
        </div>
    </section>
    
    <!-- Background Slideshow -->
    <ul class="slideshow">
        <li></li>
        <li></li>
        <li></li>
        <li></li>
        <li></li>
        <li></li>
        <li></li>
        <li></li>
        <li></li>
        <li></li>
    </ul>
    
    <!-- Footer -->
    <footer class="landing-footer">
        <div class="container" style="max-width: 1200px;">
            <div class="footer-content">
                <div class="footer-brand">
                    <img src="/favicon.png" alt="BuenaVista" width="24" height="24" class="d-inline-block align-text-top me-1">
                    BuenaVista
                </div>
                <div class="footer-links">
                    <a href="/locations">Explore</a>
                    <a href="/login">Login</a>
                    <a href="/register">Sign Up</a>
                </div>
                <div class="footer-social">
                    <a href="#" aria-label="Facebook"><i class="bi bi-facebook"></i></a>
                    <a href="#" aria-label="Twitter"><i class="bi bi-twitter-x"></i></a>
                    <a href="#" aria-label="Instagram"><i class="bi bi-instagram"></i></a>
                </div>
            </div>
            <div class="footer-bottom">
                &copy; <%= new Date().getFullYear() %> BuenaVista. All rights reserved.
            </div>
        </div>
    </footer>
    
    <!-- Bootstrap 5 JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Toast & Theme Toggle Script -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const toastElList = document.querySelectorAll('.toast');
            toastElList.forEach(function(toastEl) {
                const toast = new bootstrap.Toast(toastEl, {
                    animation: true,
                    autohide: true,
                    delay: toastEl.classList.contains('toast-error') ? 5000 : 4000
                });
                toast.show();
            });
            
            // Theme Toggle Functionality
            const themeToggle = document.getElementById('themeToggle');
            if (themeToggle) {
                themeToggle.addEventListener('click', function() {
                    const currentTheme = document.documentElement.getAttribute('data-theme');
                    const newTheme = currentTheme === 'dark' ? 'light' : 'dark';
                    
                    document.documentElement.setAttribute('data-theme', newTheme);
                    localStorage.setItem('theme', newTheme);
                    
                    // Add a little animation to the button
                    this.style.transform = 'rotate(360deg)';
                    setTimeout(() => {
                        this.style.transform = '';
                    }, 300);
                });
            }
        });
    </script>
    
    <style>
        /* Theme Toggle for Landing Page */
        .theme-toggle-landing {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 38px;
            height: 38px;
            border: 2px solid rgba(255, 255, 255, 0.3);
            border-radius: 50%;
            background: transparent;
            color: #fff;
            cursor: pointer;
            transition: all 0.3s ease;
            margin: 0 10px;
        }
        
        .theme-toggle-landing:hover {
            background: rgba(255, 255, 255, 0.15);
            border-color: rgba(255, 255, 255, 0.5);
            transform: rotate(15deg);
        }
        
        .theme-toggle-landing i {
            font-size: 1rem;
        }
        
        .theme-toggle-landing .bi-sun-fill {
            display: none;
        }
        
        .theme-toggle-landing .bi-moon-fill {
            display: block;
        }
        
        [data-theme="dark"] .theme-toggle-landing .bi-sun-fill {
            display: block;
        }
        
        [data-theme="dark"] .theme-toggle-landing .bi-moon-fill {
            display: none;
        }
        
        /* Toast styles for landing page */
        .toast {
            min-width: 320px;
            border: none;
            border-radius: 12px;
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.3);
            overflow: hidden;
            animation: slideInRight 0.4s ease-out;
        }
        
        .toast-header {
            border-bottom: none;
            padding: 12px 16px;
        }
        
        .toast-body {
            padding: 12px 16px;
            font-weight: 500;
            background: #fff;
        }
        
        .toast-error .toast-body {
            border-left: 4px solid #E76F51;
        }
        
        .toast-success .toast-body {
            border-left: 4px solid #4CAF50;
        }
        
        @keyframes slideInRight {
            from {
                transform: translateX(100%);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }
        
        @media (max-width: 575.98px) {
            .toast-container {
                left: 0 !important;
                right: 0 !important;
                padding: 16px !important;
            }
            
            .toast {
                min-width: 100%;
                width: 100%;
            }
        }
    </style>
</body>
</html>
