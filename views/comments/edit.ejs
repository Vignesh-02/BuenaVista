<%- include("../partials/header"); %>

<div class="container" style="max-width: 1200px;">
    <!-- Back Button -->
    <a href="/locations/<%= location_id %>" class="btn btn-camp-secondary mb-4">
        <i class="bi bi-arrow-left me-2"></i>Back to Location
    </a>
    
    <div class="row justify-content-center">
        <div class="col-lg-5 col-md-7">
            <div class="form-custom">
                <div class="text-center mb-4">
                    <div class="d-inline-flex align-items-center justify-content-center rounded-circle mb-3" style="width: 60px; height: 60px; background: var(--color-warning);">
                        <i class="bi bi-pencil text-white" style="font-size: 1.5rem;"></i>
                    </div>
                    <h2 class="page-title mb-2">Edit Comment</h2>
                    <p class="text-muted">Update your comment</p>
                </div>
                
                <form action="/locations/<%= location_id %>/comments/<%= comment._id %>?_method=PUT" method="POST">
                    <div class="mb-4">
                        <label for="comment" class="form-label">
                            <i class="bi bi-chat-dots me-1"></i>Your Comment
                        </label>
                        <textarea 
                            class="form-control" 
                            id="comment" 
                            name="comment[text]" 
                            rows="4"
                            placeholder="Share your thoughts..."
                            required
                        ><%= comment.text %></textarea>
                    </div>
                    
                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-camp-primary btn-lg">
                            <i class="bi bi-check-lg me-2"></i>Update Comment
                        </button>
                    </div>
                </form>
                
                <div class="text-center mt-4">
                    <a href="/locations/<%= location_id %>" class="text-muted">
                        <i class="bi bi-x-lg me-1"></i>Cancel
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<%- include("../partials/footer"); %>
